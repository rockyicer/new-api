


## Feature Request: 支持按周导出使用记录为 CSV

### 背景

希望系统支持将使用记录按周导出为 CSV，方便做核算、统计和绩效评估。

在实际使用中，管理员通常不仅需要在线查看日志，还需要把一段时间内的使用记录导出到 Excel 或其他报表工具中进行后续处理。

### 当前问题

当前系统已有日志查询和统计能力，但如果缺少结构化 CSV 导出，就不方便用于以下场景：

- 统计不同用户或学生的用量
- 进行内部核算
- 将使用数据换算为工作时间或其他业务指标
- 每周做绩效评估

### 期望功能

希望支持将使用记录导出为 CSV，并至少支持以下筛选条件：

- 周范围
- 用户
- Token
- 模型
- 接口 / 请求路径
- 分组

### 建议导出的字段

希望 CSV 至少包含以下列：

- 使用时间
- 用户标识 / 用户名
- Token 标识 / Token 名称
- 模型名称
- 调用接口 / 请求路径
- 消耗额度 / 用量
- Prompt Tokens
- Completion Tokens
- IP 地址
- Request ID

### 建议的使用方式

在使用日志页面中增加：

- “按周”快捷筛选
- “导出 CSV”按钮

如果可以的话，也希望后续支持通过 API 导出，这样可以方便做自动化周报。

### 适用场景

这个功能会很适合：

- 学生用量统计
- 团队内部成本核算
- 每周工作量评估
- 运营报表导出
- 财务或管理侧的表格处理流程

### 补充说明

如果目前系统更偏向在线查看和图表统计，也希望能补充 CSV 导出能力，因为很多实际管理流程最终还是依赖表格工具完成。

感谢考虑这个功能。
